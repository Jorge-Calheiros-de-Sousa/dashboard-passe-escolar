export type Recarga = {
    id: string,
    partida: string
    destino: string
    total: string
    data: string
}

export type FormRecarga = {
    cartao: number | string
    total: number
    data: string
}